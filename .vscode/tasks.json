{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build",
            "type": "shell",
            "command": "export GITHUB_TOKEN=$(cat ./github_token.env) && bluebuild build ./recipes/recipe.yml -B podman -vv",
            "options": {
                "cwd": "${workspaceFolder}"
            }
        }
    ]
}